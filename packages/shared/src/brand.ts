/**
 * Identidad del producto. El nombre "IF Nodes" es provisional:
 * cambiar acá (y los tokens CSS en apps/web/app/globals.css) renombra todo.
 */
export const BRAND = {
  name: 'IF Nodes',
  shortName: 'IFN',
  tagline: 'Builder interno de bots y automatizaciones',
  company: 'Equipo IF',
} as const;
